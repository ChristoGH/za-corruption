# plans/archive

Spent or wrong-project plans, preserved for reference. Each entry records why it was
retired; see the file itself for the original content.

| File | Retired reason |
|------|---------------|
| `prompt_17_cross_model_diff_harness.md` | Spent — Gate 2 cross-model eval implemented and run (40-chunk stratified sample, seed 17); artifacts in `eval/artifacts/`. |
| `prompt_repo_ground_truth_audit.md` | Wrong project nouns — written against a different repo layout (`video_pipeline/`, `plan/` dir, `STATE.md`); no relevant code was produced from it in this repo. |
| `video_acquisition_slice_V0-V3.md` | Wrong project — no video track exists here; the Madlanga/Zondo commission is a transcript-PDF corpus only. |
| `BUILD_PLAN.md` | Forks source of truth — described a `plan/` dir (not `plans/`) and a three-prompt sequence that included the video track; superseded by `docs/project-state.md §8` as the single plan-of-record. |
