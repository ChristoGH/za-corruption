"""Cross-model extraction evaluation — Gate 2 decision support.

Measures, instead of guessing, what the cheaper models lose relative to the
Opus silver reference on the axes this corpus actually fails on: ambiguous
surnames, OCR-variant counsel labels, acronym density, and neutral
reported-speech framing. A human makes the model call; this package makes the
call measurable.
"""
